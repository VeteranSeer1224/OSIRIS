name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - name: Install system dependencies (WeasyPrint)
        run: |
          sudo apt-get update
          sudo apt-get install -y libpango-1.0-0 libpangocairo-1.0-0
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Pre-install pinned CPU-only PyTorch to avoid downloading 6GB of CUDA wheels in CI
          pip install torch==2.12.1+cpu --index-url https://download.pytorch.org/whl/cpu
          pip install -r requirements.txt
      - name: Run tests
        run: |
          python -m compileall -q .
          pytest -v
      - name: Verify local evidence CLI surface
        run: |
          python osiris.py --json --help
